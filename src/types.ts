export type TStar = {
  x: number;
  y: number;
  size: number;
};

export type TEnemy = {
  x: number;
  y: number;
  size: number;
  type: number;
};

export type TShot = {
  x: number;
  y: number;
};

export type TRectSize = {
  width: number;
  height: number;
};

export type TGameInfo = { isPlaying: boolean };
